export const eq = (value1: any, value2: any): boolean => {
    return value1 == value2
}
export const not = (value: boolean): boolean => {
    return !value
}

export const and = (value1: boolean, value2: boolean) => {
    return value1 && value2
}
