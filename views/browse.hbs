{{>menuBarItems}}

<div class="item-container">
    {{>browseItemForm}}

    {{#if items}}
        {{>itemList}}
    {{else}}
        <a class="small-button" href="/resources/getFromArxiv">Search in arxiv</a>
    {{/if}}
</div>

{{#if message}}
    {{>messageBar}}
{{/if}}